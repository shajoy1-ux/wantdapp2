import { SellerApplyClient } from './SellerApplyClient'

export const metadata = {
  title: 'Become a Verified Seller — wantd.in',
  description: 'Apply to become a verified seller on wantd.in. Get access to buyer requests and start winning deals.',
}

export default function SellerApplyPage() {
  return <SellerApplyClient />
}
